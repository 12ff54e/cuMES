# Build artifacts (build/ = verify preset; build-* = the other CMakePresets.json
# configs: float, debug, sanitize, nobackend, profiling, and the one-off
# backend-matrix dirs).
build/
build-*/

# Run output
cumes_state.bin
dump/

# Generated measurements and visualizations
benchmark-results/
figure_data/
profiles/
*.ncu-rep
*.nsys-rep

# Per-change verification scratch (baselines live in perf_base/)
perf_check/
perf_refactor_pre/
perf_refactor_post/

# Editors / misc
*.swp
*~
.verify-scratch/
